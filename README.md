# POI_image_classification

Environment: install from env.yml

# Predict
Input requiment:
 - Csv file of list image to predict, example 123.png
Output:
 
call function from signboard_detect.py
```sh
inference_signboard(path_to_image)
```
